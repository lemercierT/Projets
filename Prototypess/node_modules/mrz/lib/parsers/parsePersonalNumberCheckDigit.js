'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
exports.parsePersonalNumberCheckDigit = void 0;
const check_1 = require("./check");
const cleanText_1 = require("./cleanText");
function parsePersonalNumberCheckDigit(checkDigit, personalNumber) {
    const cleanNumber = (0, cleanText_1.cleanText)(personalNumber);
    if (cleanNumber === '') {
        if (checkDigit !== '<' && checkDigit !== '0') {
            throw new Error(`invalid check digit ${checkDigit}: must be 0 or <`);
        }
        else {
            return checkDigit;
        }
    }
    (0, check_1.check)(personalNumber, checkDigit);
    return checkDigit;
}
exports.parsePersonalNumberCheckDigit = parsePersonalNumberCheckDigit;
//# sourceMappingURL=parsePersonalNumberCheckDigit.js.map