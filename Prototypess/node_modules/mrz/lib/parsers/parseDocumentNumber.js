'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const cleanText_1 = require("./cleanText");
function parseDocumentNumber(source, checkDigit, optional) {
    let end, value;
    if (checkDigit === '<' && optional) {
        const firstFiller = optional.indexOf('<');
        const tail = optional.substring(0, firstFiller - 1);
        value = source + tail;
        end = value.length + 1;
    }
    else {
        value = (0, cleanText_1.cleanText)(source);
        end = value.length;
    }
    return {
        value,
        start: 0,
        end,
    };
}
exports.default = parseDocumentNumber;
//# sourceMappingURL=parseDocumentNumber.js.map