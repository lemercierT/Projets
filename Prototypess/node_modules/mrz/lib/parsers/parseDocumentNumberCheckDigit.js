'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const check_1 = require("./check");
function parseDocumentNumberCheckDigit(checkDigit, source, optional) {
    if (checkDigit === '<' && optional) {
        const firstFiller = optional.indexOf('<');
        const tail = optional.substring(0, firstFiller - 1);
        source = `${source}<${tail}`;
        checkDigit = optional.charAt(firstFiller - 1);
        (0, check_1.check)(source, checkDigit);
        return {
            value: checkDigit,
            start: firstFiller,
            end: firstFiller + 1,
        };
    }
    else {
        (0, check_1.check)(source, checkDigit);
        return checkDigit;
    }
}
exports.default = parseDocumentNumberCheckDigit;
//# sourceMappingURL=parseDocumentNumberCheckDigit.js.map