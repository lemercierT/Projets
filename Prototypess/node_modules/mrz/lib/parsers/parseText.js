'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseText = void 0;
const cleanText_1 = require("./cleanText");
function parseText(source, regexp = /^[0-9A-Z<]+$/) {
    if (!source.match(regexp)) {
        throw new Error(`invalid text: ${source}. Must match the following regular expression: ${regexp.toString()}`);
    }
    return (0, cleanText_1.cleanText)(source);
}
exports.parseText = parseText;
//# sourceMappingURL=parseText.js.map